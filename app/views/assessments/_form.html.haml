= form_with(model: assessment, local: true) do |form|
  - if assessment.errors.any?
    #error_explanation
      %h2
        = pluralize(assessment.errors.count, "error")
        prohibited this assessment from being saved:
      %ul
        - assessment.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name, requried: true, maxlength: 30
  .field
    = form.label :weight
    = form.number_field :weight, required: true, min: 0, max: 1
  .field
    = form.label :course_id
    = form.number_field :course_id
  .actions
    = form.submit
